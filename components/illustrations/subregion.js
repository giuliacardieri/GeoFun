import subRegionStyles from "@/styles/illustrations/subregion.module.css";

export default function Subregion(props) {
  return (
    <div className={subRegionStyles.subregion}>
      <div className={subRegionStyles.subregion__circle}></div>
    </div>
  );
}
